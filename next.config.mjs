/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    images: {
        // domains: ['ecommerce.ssg-tech.info'],
        unoptimized: true,
    },
};

export default nextConfig;
